server:
  port: 8761
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka, https://${eureka.instance.hostname}:${server.port}/eureka/  
